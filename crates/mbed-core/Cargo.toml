[package]
name = "mbed-core"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
mbed-macros = { version = "0.1.0", path = "../mbed-macros" }
mime_guess = "2.0.5"
tanager = "0.3.0"

[features]
cargo-progress = [ "mbed-macros/cargo-progress" ]
